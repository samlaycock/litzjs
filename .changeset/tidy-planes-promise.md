---
"litzjs": minor
---

Link Vite-emitted CSS assets from production document responses, resolve the browser entry from Vite's `index.html` module script, and remove the `clientEntry` plugin option.
